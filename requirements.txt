# Web framework
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27

# Templates
jinja2==3.1.6

# Database
sqlalchemy==2.0.49
alembic==1.18.4

# Config / env
pydantic-settings==2.14.0
python-dotenv==1.2.2

# TLS: verify outbound HTTPS against the OS trust store. Lets dev machines behind
# an HTTPS-intercepting proxy/AV reach the LLM APIs; harmless on Render/Linux.
truststore==0.10.4

# Google integration
gspread==6.2.1
google-auth==2.50.0
google-auth-oauthlib==1.3.1

# AI / Lead generation
# Note: Ollama is a system-level install (https://ollama.com), not a pip package.
# The openai SDK below handles all calls to the Ollama local REST endpoint.
anthropic==0.100.0
openai==2.36.0
tavily-python==0.7.24
duckduckgo-search==8.1.1

# Timezone database (required on Windows for zoneinfo)
tzdata==2026.2

# Auth
authlib==1.7.2
itsdangerous==2.2.0

# HTML parsing (price scraper)
beautifulsoup4==4.14.3
lxml==6.1.0

# Dev / test
pytest==9.0.3
httpx==0.28.1
ruff==0.15.12
