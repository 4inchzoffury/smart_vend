"""Archived price fetchers — kept for historical reference, not actively imported.

Modules under this package are deliberately not exported by the parent
`price_fetcher` package. They were retired when their underlying vendor pages
stopped supporting the scraping approach used here. The code stays in tree so
git history is intact and we can resurrect a fetcher if the vendor's markup
returns to something tractable.
"""
